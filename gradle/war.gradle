apply plugin: "war"

bootWar {
    mainClassName = "com.mycompany.myapp.GradletestprojectApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
